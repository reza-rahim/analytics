[Unit]
Description=Patroni
After=network.target 

[Service]

User={{ db_user }}
Group={{ application_group }}
ExecStart=/bin/bash -c "/usr/local/bin/patroni  /opt/patroni/patroni-node.yaml"
Restart=on-failure

[Install]
WantedBy=multi-user.target
